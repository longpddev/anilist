import { useState } from "react";
function App() {
  const [count] = useState(0);

  return (
    <div
      className="sdf sdf sdf s"
      onClick={() => {}}
      title="12 "
    >
      Hello world
    </div>
  );
}

export default App;
